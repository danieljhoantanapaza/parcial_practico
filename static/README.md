# Portal Académico

Aplicación web desarrollada con Python y Flask que simula el acceso de estudiantes a un portal académico.

## Instalación

```bash
python -m venv venv
venv\Scripts\activate
pip install Flask